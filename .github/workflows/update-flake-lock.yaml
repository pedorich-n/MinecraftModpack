name: Update flake.lock
on:
  workflow_dispatch: # allows manual triggering
  schedule:
    - cron: "0 10 1,14 * *"

jobs:
  update-flake-lock:
    uses: pedorich-n/github-workflows/.github/workflows/update-flake-lock.yaml@main
    secrets:
      gh_access_token: ${{ secrets.JOAQIM_CI_BOT_GITHUB_TOKEN }}
